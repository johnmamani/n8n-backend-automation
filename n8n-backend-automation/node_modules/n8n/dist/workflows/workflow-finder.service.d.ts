import type { Scope } from '@n8n/permissions';
import type { EntityManager } from '@n8n/typeorm';
import type { SharedWorkflow } from '../databases/entities/shared-workflow';
import type { User } from '../databases/entities/user';
import { SharedWorkflowRepository } from '../databases/repositories/shared-workflow.repository';
import { RoleService } from '../services/role.service';
export declare class WorkflowFinderService {
    private readonly sharedWorkflowRepository;
    private readonly roleService;
    constructor(sharedWorkflowRepository: SharedWorkflowRepository, roleService: RoleService);
    findWorkflowForUser(workflowId: string, user: User, scopes: Scope[], options?: {
        includeTags?: boolean;
        includeParentFolder?: boolean;
        em?: EntityManager;
    }): Promise<import("../databases/entities/workflow-entity").WorkflowEntity | null>;
    findAllWorkflowsForUser(user: User, scopes: Scope[]): Promise<{
        projectId: string;
        name: string;
        active: boolean;
        nodes: import("n8n-workflow").INode[];
        connections: import("n8n-workflow").IConnections;
        settings?: import("n8n-workflow").IWorkflowSettings;
        staticData?: import("n8n-workflow").IDataObject;
        meta?: import("n8n-workflow").WorkflowFEMeta;
        tags?: import("../databases/entities/tag-entity").TagEntity[];
        tagMappings: import("../databases/entities/workflow-tag-mapping").WorkflowTagMapping[];
        shared: SharedWorkflow[];
        statistics: import("../databases/entities/workflow-statistics").WorkflowStatistics[];
        pinData?: import("../databases/entities/workflow-entity").ISimplifiedPinData;
        versionId: string;
        triggerCount: number;
        parentFolder: import("../databases/entities/folder").Folder | null;
        id: string;
        createdAt: Date;
        updatedAt: Date;
    }[]>;
}
